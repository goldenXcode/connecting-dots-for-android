package view.interfaces;

public interface IMatrixDrawer {

	/**
	 * 
	 * @param matrix
	 *            - the matrix for drawing
	 * @return - the two indexes of the item the user selected from the matrix
	 */
	void draw(String[][] matrix);

}
